# Quarterly Briefing — What Changed (and what we should do) (template)

*Generate a first draft in NotebookLM (Studio → Briefing doc), paste it here, then shape it into a note you could send the team. Every claim must trace to an `[S#]` in `sources.md`. Italic text is the target — overwrite it.*

## TL;DR for the team
`<< 2–3 sentences: the quarter's biggest shifts and the one thing we should focus on. >>`
*Example: Search traffic is squeezed by AI Overviews, paid social got pricier, and AI tooling adoption surged. Conversion is flat, so our biggest lever is converting the traffic we already get.*

## What changed (with sources)

| Change | The data | Source |
|--------|----------|--------|
| `<< change 1 >>` | `<< the number/finding >>` | `[S#]` |
| `<< change 2 >>` | `<< the number/finding >>` | `[S#]` |
| `<< change 3 >>` | `<< the number/finding >>` | `[S#]` |

## What we should do
1. `<< action tied to change 1 >>` `[S#]`
2. `<< action tied to change 2 >>` `[S#]`
3. `<< action tied to change 3 >>` `[S#]`

## Send-ready note
> `<< a 2–3 sentence Slack/email version your team can act on, ending with "full cited breakdown in the shared notebook" >>`

## Audio Overview
`<< paste your Audio Overview Share link — share a 10-minute listen instead of five PDFs >>`
